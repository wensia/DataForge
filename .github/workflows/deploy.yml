# DataForge CI/CD 工作流
# 构建 Docker 镜像 → 推送到腾讯云 TCR → 部署到服务器

name: Build and Deploy

on:
  push:
    branches: [main]
  workflow_dispatch:

env:
  REGISTRY: ccr.ccs.tencentyun.com
  NAMESPACE: pandw
  BACKEND_IMAGE: ccr.ccs.tencentyun.com/pandw/dataforge-backend
  FRONTEND_IMAGE: ccr.ccs.tencentyun.com/pandw/dataforge-frontend

jobs:
  # ===== 构建后端镜像 =====
  build-backend:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to Tencent TCR
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ secrets.TCR_USERNAME }}
          password: ${{ secrets.TCR_PASSWORD }}

      - name: Build and push backend
        uses: docker/build-push-action@v5
        with:
          context: ./backend
          file: ./backend/Dockerfile
          push: true
          tags: |
            ${{ env.BACKEND_IMAGE }}:latest
            ${{ env.BACKEND_IMAGE }}:${{ github.sha }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

  # ===== 构建前端镜像 =====
  build-frontend:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to Tencent TCR
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ secrets.TCR_USERNAME }}
          password: ${{ secrets.TCR_PASSWORD }}

      - name: Build and push frontend
        uses: docker/build-push-action@v5
        with:
          context: ./frontend-react
          file: ./frontend-react/Dockerfile
          push: true
          tags: |
            ${{ env.FRONTEND_IMAGE }}:latest
            ${{ env.FRONTEND_IMAGE }}:${{ github.sha }}
          build-args: |
            VITE_API_BASE_URL=/api/v1
          cache-from: type=gha
          cache-to: type=gha,mode=max

  # ===== 部署到服务器 =====
  deploy:
    runs-on: ubuntu-latest
    needs: [build-backend, build-frontend]

    steps:
      - name: Deploy to server via SSH
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SERVER_SSH_KEY }}
          script: |
            cd /www/wwwroot/yunke-transit

            # 拉取最新代码
            git pull origin main

            # 登录腾讯云 TCR
            sudo docker login ccr.ccs.tencentyun.com -u ${{ secrets.TCR_USERNAME }} -p '${{ secrets.TCR_PASSWORD }}'

            # 拉取最新镜像
            sudo docker pull ccr.ccs.tencentyun.com/pandw/dataforge-backend:latest
            sudo docker pull ccr.ccs.tencentyun.com/pandw/dataforge-frontend:latest

            # 使用生产配置重启服务
            sudo docker compose -f docker-compose.prod.yml down --remove-orphans || true
            sudo docker compose -f docker-compose.prod.yml up -d

            # 清理旧镜像
            sudo docker image prune -f

            # 显示状态
            sudo docker compose -f docker-compose.prod.yml ps

            echo "部署完成！"
