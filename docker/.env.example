# ========================================
# DataForge Docker 环境变量配置
# 只容器化应用服务，连接宿主机数据库
# 复制此文件为 .env 并修改配置
# ========================================

# ===== 数据库连接（宿主机 PostgreSQL）=====
# host.docker.internal 指向宿主机
DATABASE_URL=postgresql://postgres:你的数据库密码@host.docker.internal:5432/production

# ===== Redis 连接（宿主机 Redis）=====
REDIS_URL=redis://:你的Redis密码@host.docker.internal:6379/0

# ===== 后端应用配置 =====
# 基础配置
APP_NAME=DataForge
APP_VERSION=0.1.0
DEBUG=false

# API 配置
API_PREFIX=/api/v1
API_KEYS=df_your_secure_api_key_here

# JWT 配置
JWT_SECRET_KEY=your_super_secure_jwt_secret_key_change_this
JWT_EXPIRE_HOURS=24

# 调度器配置
SCHEDULER_ENABLED=true
TIMEZONE=Asia/Shanghai
MAX_EXECUTION_HISTORY_DAYS=30

# 安全审计
ENABLE_SECURITY_AUDIT=true
SECURITY_LOG_FILE=logs/security.log

# ===== AI 服务配置 =====
KIMI_API_KEY=your-kimi-api-key
DEEPSEEK_API_KEY=your-deepseek-api-key
DEFAULT_AI_PROVIDER=kimi

# ===== 缓存配置 =====
RECORD_CACHE_TTL=180
API_KEY_CACHE_TTL=300
